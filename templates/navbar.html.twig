
    {% block navbar %}

        <nav class=" fixed top-0 left-1/2 z-10 flex -translate-x-1/2 items-center justify-between bg-white bg-opacity-25 backdrop-filter backdrop-blur-lg rounded-lg p-4 flex items-center border border-gray-300">
            <div class="flex items-center">
                <a href="{{ path('app_index') }}" class="text-gray-800 font-bold text-xl mr-4">Accueil</a>
                <ul class="flex space-x-4">
                    <li><a href="{{ path('picture_list') }}" class="text-gray-800 hover:text-gray-600">Images</a></li>
                    <li><a href="{{ path('app_contact') }}" class="text-gray-800 hover:text-gray-600">Contact</a></li>
                    <li><a href="{{ path('app_login') }}" class="text-gray-800 hover:text-gray-600">Connexion</a></li>
                </ul>
            </div>
        </nav>

    {% endblock %}
