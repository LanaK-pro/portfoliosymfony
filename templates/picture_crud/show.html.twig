{% extends 'base.html.twig' %}

{% block title %}Picture{% endblock %}

{% block body %}
    <h1>Picture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picture.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ picture.title }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ picture.url }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ picture.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_picture_crud_index') }}">back to list</a>

    <a href="{{ path('app_picture_crud_edit', {'id': picture.id}) }}">edit</a>

    {{ include('picture_crud/_delete_form.html.twig') }}
{% endblock %}
